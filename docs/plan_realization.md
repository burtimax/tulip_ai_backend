# Dev Plan (MVP): Chat processing backend

Документ адаптирован под текущую структуру проекта и режим разработки чата с асинхронной обработкой сообщений (текст/изображения) через PlantId + OpenRouterService.

## Текущая структура, на которую опирается план

- `Api` — вход в систему, middleware, endpoints, DI, конфигурация.
- `Application` — сервисы и бизнес-логика use-case уровня.
- `Infrastructure` — EF Core, `AppDbContext`, сущности, миграции, persistence.
- `Shared` — конфиги, контракты результата, модели, утилиты.
- `SharedServices` — общие сервисы и внешние интеграции.
- `ConsoleTest` — тестовые/вспомогательные консольные сценарии.

## Правила ведения плана

- [ ] Выполнять эпики последовательно с учётом зависимостей.
- [ ] Перед началом задачи менять чекбокс на `[-]`.
- [ ] После завершения ставить `[x]` и фиксировать артефакт (PR/док/миграция/тест).
- [ ] Если требуется решение по архитектуре — помечать `[!]`.

Статусы:
- `[ ]` не начато
- `[-]` в работе
- `[x]` завершено
- `[!]` блокер

---

## Epic 1. Scope чата и спецификации MVP

**Цель:** зафиксировать контракт и поведение чат-сервиса с асинхронной обработкой сообщений.

### 1.1 Scope Chat-only MVP
- [x] Утвердить границы MVP: история чатов, создание чата, отправка сообщений, асинхронная обработка.
- [x] Зафиксировать типы сообщений: только текст (HTML), только изображения, смешанный режим.
- [x] Утвердить минимальные сценарии: успешная обработка, ошибка PlantId, ошибка LLM, повторная попытка.

### 1.2 Контракты сущностей и статусов
- [x] Утвердить сущности: `Chat`, `Message`, `MessageImage`, `ProcessingJob`.
- [x] Утвердить статус-машину `Message`: `Queued -> Processing -> Completed/Failed`.
- [x] Утвердить агрегированный `Chat.Status`: `Idle/Processing/Error`.

### 1.3 Алгоритм обработки сообщений (канонический)
- [x] Зафиксировать правило: если есть изображения, обрабатывать каждое последовательно.
- [x] Утвердить использование PlantId `create identification` по каждому изображению.
- [x] Утвердить правило выбора top-гипотез по растению и болезням.
- [x] Утвердить агрегацию результатов изображений в единый контекст для prompt.
- [x] Утвердить формирование prompt: контекст PlantId + текст пользователя (если есть) + системные инструкции.
- [x] Утвердить финализацию: запись ответа `Assistant`, завершение статусов, публикация пользователю.

### 1.4 API v1 для чата
- [x] Утвердить endpoints: `POST /chats`, `GET /chats`, `GET /chats/{chatId}`.
- [x] Утвердить endpoints сообщений: `POST /chats/{chatId}/messages`, `GET /chats/{chatId}/messages`.
- [x] Утвердить контракт ответа на отправку: `messageId`, `status=Queued`, `createdAt`.
- [x] Утвердить формат ошибок и валидации.

**DoD эпика:** спецификация Chat MVP v1 согласована и готова к реализации.  
Артефакт: `docs/chat_mvp_v1_spec.md`.

---

## Epic 2. Подготовка solution под чат-обработку

**Зависимость:** Epic 1.

### 2.1 Архитектурное выравнивание по слоям
- [x] Зафиксировать ответственность слоёв `Api` / `Application` / `Infrastructure` / `Shared`.
- [x] Утвердить место реализации worker-а (hosted service в `Api` или отдельный процесс).
- [x] Подготовить ADR по модели очереди и порядку обработки.

### 2.2 Конфигурации и environment
- [x] Добавить конфиг очереди, PlantId, OpenRouterService, storage.
- [x] Описать env vars/секреты и политику их хранения.
- [x] Добавить fail-fast валидацию конфигурации при старте.

### 2.3 Базовые cross-cutting механизмы
- [x] Добавить структурированные логи.
- [x] Добавить корреляцию по `chatId`/`messageId`/`jobId`.
- [x] Утвердить единый формат ошибок API/worker.

**DoD эпика:** solution готов к реализации пайплайна чата и внешних интеграций.  
Артефакты: `docs/adr_epic2_chat_architecture.md`, `docs/env_chat_mvp.md`, `Api/Middleware/RequestCorrelationMiddleware.cs`, `Shared/Contracts/ApiErrorContract.cs`.

---

## Epic 3. Модель данных и миграции в `Infrastructure`

**Зависимость:** Epic 2.

### 3.1 Модель данных chat-domain
- [x] Спроектировать и утвердить ER-модель `Chat`, `Message`, `MessageImage`, `ProcessingJob`.
- [x] Утвердить nullable/required поля и хранение HTML/PlantId raw.
- [x] Утвердить стратегию хранения изображений (URL + метаданные).

### 3.2 Реализация в `AppDbContext`
- [x] Добавить `DbSet` и конфигурации сущностей.
- [x] Настроить связи и ограничения целостности.
- [x] Настроить каскадные правила удаления/архивирования по бизнес-логике.

### 3.3 Индексы и уникальность
- [x] Индекс `Chat(UserId, UpdatedAt desc)` для истории.
- [x] Индекс `Message(ChatId, CreatedAt)` для ленты сообщений.
- [x] Индекс `ProcessingJob(Status, CreatedAt)` для выборки worker-ом.
- [x] Уникальность идемпотентности (например, `Message.ClientRequestId` при наличии).

### 3.4 Миграции
- [x] Сформировать миграции EF Core.
- [x] Проверить применение миграций локально и на старте приложения.
- [x] Обновить документацию по migration-командам.

**DoD эпика:** схема БД чата описана и применима миграциями.  
Артефакты: `docs/epic3_chat_data_model.md`, `Infrastructure/Db/App/Migrations/20260423165514_AddChatDomainModel.cs`.

---

## Epic 4. Приём сообщений и очередь

**Зависимость:** Epic 2 и Epic 3.

### 4.1 API отправки сообщений
- [x] Реализовать `POST /chats/{chatId}/messages`.
- [x] Реализовать валидацию: сообщение должно содержать текст, изображения или оба.
- [x] Добавить санитизацию HTML (whitelist-теги).
- [x] Добавить ограничения размера/типа/кол-ва изображений.

### 4.2 Persistence и enqueue
- [x] Сохранять сообщение со статусом `Queued`.
- [x] Сохранять изображения и метаданные (`MessageImage`).
- [x] Создавать `ProcessingJob` и возвращать быстрый ответ (`200/202`).
- [x] Обновлять статус чата в `Processing`, если есть активные задачи.

### 4.3 Канал доставки статусов клиенту
- [x] Реализовать polling для получения новых сообщений/статусов.
- [ ] (Опционально) спроектировать SSE/WebSocket для realtime.

**DoD эпика:** пользователь может отправить сообщение, а система гарантированно ставит обработку в очередь.  
Артефакты: `Api/Endpoints/Chat/SendMessageEndpoint.cs`, `Api/Endpoints/Chat/GetMessagesEndpoint.cs`, `Application/Services/Chat/ChatService.cs`.

---

## Epic 5. Worker pipeline в `Application`

**Зависимость:** Epic 3 и Epic 4.

### 5.1 Оркестрация статусов
- [x] Переключение `Queued -> Processing -> Completed/Failed`.
- [x] Корректное выставление `Chat.Status` по агрегату сообщений.
- [x] Защита от повторной обработки завершенной задачи.

### 5.2 Обработка изображений через PlantId
- [x] Для каждого изображения последовательно вызывать PlantId `create identification`.
- [x] Извлекать top-гипотезы по растению и болезням.
- [x] Сохранять normalized + raw результат по каждому изображению.
- [x] Агрегировать результаты по всем изображениям сообщения.

### 5.3 Формирование и отправка prompt в LLM
- [x] Формировать prompt из агрегированного PlantId-контекста.
- [x] Добавлять пользовательский текст в prompt (если есть).
- [x] Отправлять запрос в `OpenRouterService`.
- [x] Сохранять ответ как `Assistant` message в текущий чат.

### 5.4 Финализация
- [x] Помечать исходное сообщение как `Completed`.
- [x] При ошибке корректно помечать `Failed` и фиксировать причину.
- [x] Обновлять `Chat.UpdatedAt`/`LastMessageAt`.

**DoD эпика:** worker полностью обрабатывает сообщение и добавляет ответ ассистента в чат.  
Артефакты: `Api/BackgroundServices/ChatProcessingWorker.cs`, `Application/Services/Chat/ChatProcessingService.cs`.

---

## Epic 6. API чтения и UX-статусы

**Зависимость:** Epic 5.

### 6.1 История и лента чата
- [x] Реализовать `GET /chats` (история с пагинацией/сортировкой).
- [x] Реализовать `GET /chats/{chatId}` (мета чата + текущий статус).
- [x] Реализовать `GET /chats/{chatId}/messages` (лента, пагинация, порядок).

### 6.2 Контракты ответа
- [x] Единый формат DTO для `User/Assistant/System` сообщений.
- [x] Возврат статусов `Message`/`Chat` в каждом read-ответе.
- [x] Единый формат ошибок и валидационных ответов.

### 6.3 UX-индикаторы
- [x] Отдавать признак "в обработке" для чата.
- [x] Отдавать пользователю признак ошибки обработки по сообщению.
- [x] Утвердить поведение для повторной отправки/повторной обработки (если требуется в MVP).

**DoD эпика:** клиент корректно отображает историю, статусы и ответы ассистента.  
Артефакты: `Api/Endpoints/Chat/GetChatsEndpoint.cs`, `Api/Endpoints/Chat/GetChatByIdEndpoint.cs`, `Api/Endpoints/Chat/GetMessagesEndpoint.cs`, `Api/Endpoints/Chat/ChatEndpointErrors.cs`.

---

## Epic 7. Надёжность и эксплуатация

**Зависимость:** Epic 5 и Epic 6.

### 7.1 Ошибки и recovery
- [x] Добавить таймауты и retry/backoff для PlantId и OpenRouterService.
- [x] Ограничить число попыток и переводить задачи в `Failed`.
- [x] Добавить стратегию dead-letter/replay (если используется внешняя очередь).

### 7.2 Наблюдаемость
- [x] Метрики: queue wait, processing time, PlantId latency, LLM latency, fail rate.
- [x] Логи по шагам pipeline с корреляторами.
- [x] Алерты на рост задержек и ошибок.

### 7.3 Безопасность и лимиты
- [x] Rate-limit на отправку сообщений.
- [x] Лимиты размера/количества вложений.
- [x] Проверка MIME и безопасная обработка входного HTML.

### 7.4 Эксплуатационные инструкции
- [x] Runbook по ошибкам внешних интеграций.
- [x] Runbook по очистке/повторной обработке зависших задач.

**DoD эпика:** чат-пайплайн готов к стабильной эксплуатации в MVP.  
Артефакты: `Api/Middleware/ChatMessageRateLimitMiddleware.cs`, `Application/Services/Chat/ChatProcessingMetrics.cs`, `Api/Endpoints/Chat/ReplayFailedJobEndpoint.cs`, `docs/epic7_runbook.md`.

---

## Epic 8. Тестирование и приемка Chat MVP

**Зависимость:** Epic 3-7.

### 8.1 Unit tests
- [ ] Валидация сообщений (текст/изображения/оба).
- [ ] Санитизация HTML и бизнес-ограничения.
- [ ] Маппинг PlantId и выбор top-гипотез.
- [ ] Prompt-builder.

### 8.2 Integration tests
- [ ] API отправки сообщения + сохранение в БД + enqueue.
- [ ] Worker pipeline (успех/ошибка/retry).
- [ ] Проверка статусов чата и сообщений в read API.

### 8.3 E2E tests
- [ ] Сценарий: только текст -> ответ LLM.
- [ ] Сценарий: только изображения -> PlantId -> LLM -> ответ.
- [ ] Сценарий: текст + несколько изображений -> последовательная обработка -> ответ.
- [ ] Сценарий: сбой внешнего сервиса -> корректный `Failed` + UX-индикатор.

### 8.4 Приемка
- [ ] Пользователь может создавать чат и открывать историю.
- [ ] Пользователь отправляет сообщения и сразу получает ответ API.
- [ ] Ответ ассистента появляется после асинхронной обработки.
- [ ] Статусы сообщений/чатов отображаются корректно.
- [ ] Логи/метрики достаточны для диагностики.

**DoD эпика:** Chat MVP принят.

---

## Очередность работ для агентов

- [x] Волна A: `Epic 1` (спеки и контракты).
- [x] Волна B: `Epic 2 + Epic 3` (архитектурная подготовка + БД).
- [x] Волна C: `Epic 4 + Epic 5` (API отправки + worker pipeline).
- [x] Волна D: `Epic 6` (read API + UX-статусы).
- [ ] Волна E: `Epic 7 + Epic 8` (hardening + тесты + приемка).

Правило: переход к следующей волне только после `DoD` предыдущей.

## Baseline

- В `DomainParser.sln` присутствуют целевые проекты backend-слоёв: `Api`, `Application`, `Infrastructure`, `Shared`, `SharedServices`, `ConsoleTest`.
- Базовая композиция в `Api` уже может быть использована как host для chat endpoints и фонового worker-а.
- В `Infrastructure` уже есть база для развития EF Core модели (контекст/миграции/конвенции), которую нужно расширить chat-сущностями.
- Спецификация чат-пайплайна (PlantId -> prompt -> OpenRouterService -> ответ в чат) зафиксирована в этом документе и служит источником реализации.
- Стратегии retry/наблюдаемости и окончательные операционные runbook требуют доведения в Epic 7.
